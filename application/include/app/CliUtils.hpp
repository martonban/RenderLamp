#pragma once

enum CliViewMode {
    SERVER_VIEW,
    CLI_APP_VIEW
};

enum CliAppScenes {
    WELCOME_SCREEN,
    MAIN_MENU,
    ADD_PROJECTS,
    SELECT_PROJECT,
    EXIT_APP,
    RENDERING_PROCESS    
};
