# Tests CMakeLists.txt
cmake_minimum_required(VERSION 3.10)

# Add test subdirectories
add_subdirectory(Arca)
# add_subdirectory(editor)     # Uncomment when editor tests are added
# add_subdirectory(renderer)   # Uncomment when renderer tests are added
